The service database role (i.e. user) and schema are initially created when the postgres database for the Tapis
 service is first deployed. The script containing SQL is run using a kubernetes job.

In a Tapis deployment the files may be found under tapis-kube/systems/postgres_16.
The two main files involved are *systems-init-db-sh* and *systems-init-db.yml*.
Burndown involves:
  kubectl delete -f systems-init-db.yml
Burnup involves:
  kubectl create configmap systems-init-db-configmap --from-file systems-init-db-sh
  kubectl create -f systems-init-db.yml
  kubectl wait --timeout=120s --for=condition=complete job/systems-init-db

For reference copies of the two files are maintained here in this directory.
NOTE that these copies may not have been updated recently and so may not match what is currently used as
part of tapis-deployer to create the DB initially.

Each time the service starts up a flyway migration is run to bring the DB schema up to date as needed.
This happens as part of application (SystemsApplication.java) startup.
The calling sequence is:
   SystemsApplication.svc.initService -> SystemsServiceImpl.dao.migrateDB() -> SystemsDaoImpl.flyway.migrate()